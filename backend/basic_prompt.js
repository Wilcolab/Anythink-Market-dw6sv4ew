/**
 * Write a function `toCamelCase` that converts a given string to camelCase.
 * The function should handle strings with spaces, underscores, or hyphens as word separators.
 * 
 * Example:
 * toCamelCase("hello world") -> "helloWorld"
 * toCamelCase("convert_to_camel_case") -> "convertToCamelCase"
 * toCamelCase("make-this-camel-case") -> "makeThisCamelCase"
 */